int adder = 5;

fn atoi(char* s) -> int {
    int result = 0;
    int i = 0;
    while (s[i] != 0) {
        result = result * 10 + (s[i] - '0');
        i += 1;
    }
    ret result + adder;
}

fn print(char* a) -> void {
    puts(a);
}

fn print(int a) -> void {
    puts("attempt?, nein!");
}